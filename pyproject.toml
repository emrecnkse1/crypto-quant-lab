[project]
name = "crypto-quant-lab"
version = "0.1.0"
description = "Local crypto quant research and execution platform"
requires-python = ">=3.13,<3.14"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
